name: 'local_bike'
version: '1.0.0'

profile: 'local_bike'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  local_bike:
    staging:
      +materialized: view
      bike:
        +schema: bike
    intermediate:
      +materialized: view
      bike:
        +schema: bike
    marts:
      +materialized: table
      +schema: marts

  +persist_docs:
    relation: true
    columns: true
